#include<iostream>
using namespace std;

 int main()
 {
     int n,f;
 
    cout<<"\nEnter a number : ";
    cin>>n;
     
     f=1;
     
  for(int i=2;i<=n; i++)
    {
        f=f*i;
    }
   cout<<"\nFactorial of "<<n<<"is : "<<f;

      return 0;
 }
